version: '3.8'
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: shareIt
      POSTGRES_USER: malika
      POSTGRES_PASSWORD: password
    ports:
      - '5440:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data: